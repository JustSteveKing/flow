name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: gofmt + vet
    runs-on: ubuntu-latest
    # Bound every job: a wedged step (a stalled toolchain/module fetch, a hung
    # native build) should fail fast with logs, not run to the 6h default and
    # end up silently `cancelled`.
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: gofmt
        run: |
          unformatted="$(gofmt -l ./internal ./cmd/flow ./cmd/flow-desktop)"
          if [ -n "$unformatted" ]; then
            echo "These files need gofmt:"
            echo "$unformatted"
            exit 1
          fi
      # cmd/flow-desktop pulls in Wails and needs the GTK/WebKit toolchain, so
      # vet the pure-Go surface here. The desktop binary is compiled in `desktop`.
      - name: go vet
        run: go vet ./internal/... ./cmd/flow

  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Build the CLI
        run: go build -o bin/flow ./cmd/flow
      - name: Test
        run: go test -race ./internal/... ./cmd/flow

  frontend:
    name: overlay frontend
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: cmd/flow-desktop/frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: cmd/flow-desktop/frontend/package-lock.json
      - run: npm ci
      # `npm run build` is `tsc && vite build`, so this typechecks too.
      - run: npm run build

  desktop:
    name: desktop binary (macOS)
    runs-on: macos-latest
    # Heaviest job: Wails links against the native WebKit/Cocoa frameworks via
    # pkg-config, so it gets the longest budget. This is the job that hung and
    # needed force-killing in the first run.
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: cmd/flow-desktop/frontend/package-lock.json
      - name: Build the overlay frontend
        working-directory: cmd/flow-desktop/frontend
        run: npm ci && npm run build
      - name: Build flow-desktop
        run: go build -o bin/flow-desktop ./cmd/flow-desktop
